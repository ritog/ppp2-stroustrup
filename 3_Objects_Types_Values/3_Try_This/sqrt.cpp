#include "../../std_lib_facilities.h"

int main() {
    cout << "Please enter a floating point value: ";
    int n;
    cin >> n;
    double dn = n;
    // double sqred = sqrt(dn);
    cout << "\nsquare root of n ==" << sqrt(dn);

    return 0;
}